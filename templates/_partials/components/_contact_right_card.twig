{% set universities = craft.entries.section('university').all() %}
{% set courses = craft.entries.section('courses').all() %}

<!-- Education Card -->
<div class="rounded-2xl border border-slate-200 bg-slate-50/60 p-5 hover:bg-slate-100 transition">
  <h3 class="font-semibold text-slate-700 mb-3 flex items-center gap-2">
    <i data-lucide="graduation-cap" class="w-4 h-4 text-slate-500"></i> {{dict['Təhsil & Kurslar']}}
  </h3>

  {# ==== UNIVERSITETLƏR ==== #}
  {% if universities|length %}
    <p class="text-xs uppercase tracking-wide text-slate-500 mb-1">{{dict['Universitetlər']}}</p>
    <ul class="space-y-1 text-slate-800 text-sm font-medium mb-3">
      {% for uni in universities %}
        <li>
          🎓 {{ uni.uniName }} — {{ uni.educationLevel }}{% if uni.years %}, {{ uni.years }}{% endif %}
        </li>
      {% endfor %}
    </ul>
  {% endif %}

  {# ==== KURSLAR ==== #}
  {% if courses|length %}
    <p class="text-xs uppercase tracking-wide text-slate-500 mb-1">{{dict['Kurslar & Təlimlər']}}r</p>
    <ul class="space-y-1 text-slate-800 text-sm font-medium">
      {% for course in courses %}
        <li>
          💻 {{ course.courseName }}
          {% if course.courseSpecialisty %} — {{ course.courseSpecialisty }}{% endif %}
          {% if course.educationYears %} ({{ course.educationYears }}){% endif %}
        </li>
      {% endfor %}
    </ul>
  {% endif %}
</div>
